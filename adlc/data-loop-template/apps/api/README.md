# apps/api

Placeholder for the platform HTTP/gRPC API service.

Add a language stack (Go / Java / TypeScript) and wire a CI lane when implementation starts. Speak Catalog via dataset revision ids from `schemas/dataset-revision/`.
