# packages/

Shared libraries and **integration adapters** live here (annotation bridge, training bridge, …).

MVP: empty on purpose. Prefer one package per adapter with contract tests against `schemas/` and `fixtures/`.
