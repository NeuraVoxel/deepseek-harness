# Integrations

One page per external system (annotation, algorithm, evaluation, training, fine-tuning).

Each page should document: auth, idempotency, error codes, owning schema versions, fixture locations, and which adapter package owns the client.

MVP: no live bridges yet — add pages when adapters land under `packages/`.
